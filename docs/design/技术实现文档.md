# MemoForge 技术实现文档

> 版本: v1.4
> 日期: 2026-03-23
> 关联文档: [PRD](./PRD.md) v1.8 | [讨论记录](./讨论.md) | [开发计划](./开发计划文档.md)

---

## 1. 系统架构

### 1.1 进程模型

MemoForge 由两个独立进程组成，通过文件系统协同：

```
┌──────────────────────────┐    ┌─────────────────────────────┐
│  memoforge (Desktop GUI) │    │  memoforge serve (MCP)      │
│  Tauri v2 + React        │    │  独立 CLI 进程               │
│                          │    │                             │
│  ┌────────────────────┐  │    │  ┌───────────────────────┐  │
│  │  React Frontend    │  │    │  │  MCP Server (stdio)   │  │
│  │  (Webview)         │  │    │  │  HTTP Server (可选)    │  │
│  └────────┬───────────┘  │    │  └───────────┬───────────┘  │
│           │ Tauri IPC    │    │              │              │
│  ┌────────▼───────────┐  │    │  ┌───────────▼───────────┐  │
│  │  Rust Backend      │  │    │  │  Rust Backend         │  │
│  │  (Tauri Commands)  │  │    │  │  (同一 library crate) │  │
│  └────────┬───────────┘  │    │  └───────────┬───────────┘  │
│           │              │    │              │              │
└───────────┼──────────────┘    └──────────────┼──────────────┘
            │                                  │
            └──────────┬───────────────────────┘
                       ▼
              ┌─────────────────┐
              │  知识库目录      │
              │  .memoforge/    │
              │  *.md files     │
              │  .git/          │
              └─────────────────┘
```

### 1.2 Rust Crate 结构

```
memoforge/
├── Cargo.toml                    # workspace root
├── crates/
│   ├── memoforge-core/           # 核心引擎 (library crate)
│   │   ├── src/
│   │   │   ├── lib.rs
│   │   │   ├── knowledge.rs      # 知识条目 CRUD
│   │   │   ├── frontmatter.rs    # YAML frontmatter 解析
│   │   │   ├── category.rs       # 分类管理
│   │   │   ├── search.rs         # 全文搜索 + 标签组合搜索
│   │   │   ├── import.rs         # 导入已有 Markdown 文件夹
│   │   │   ├── template.rs       # 模板知识库初始化
│   │   │   ├── init.rs           # 冷启动（新建/Clone/打开知识库）
│   │   │   ├── links.rs          # 双向链接解析与索引
│   │   │   ├── git.rs            # Git 操作封装
│   │   │   ├── index.rs          # Frontmatter 索引缓存
│   │   │   ├── events.rs         # 事件日志
│   │   │   ├── lock.rs           # 文件锁与并发控制
│   │   │   └── error.rs          # 统一错误类型
│   │   └── Cargo.toml
│   ├── memoforge-mcp/            # MCP Server (binary crate)
│   │   ├── src/
│   │   │   ├── main.rs           # CLI 入口 + stdio server
│   │   │   ├── tools.rs          # MCP tool 定义与处理
│   │   │   ├── http.rs           # 可选 HTTP server
│   │   │   ├── auth.rs           # HTTP 认证中间件 + Token 管理
│   │   │   └── cli.rs            # 命令行参数解析
│   │   └── Cargo.toml
│   └── memoforge-tauri/          # Tauri 桌面应用 (binary crate)
│       ├── src/
│       │   ├── main.rs           # Tauri 入口
│       │   ├── commands.rs       # Tauri IPC commands
│       │   ├── watcher.rs        # 文件系统监听
│       │   └── state.rs          # 应用状态管理
│       ├── Cargo.toml
│       └── tauri.conf.json
├── frontend/                     # React 前端（桌面/Web共用）
│   ├── src/
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── CategoryTree.tsx  # 分类树形导航
│   │   │   ├── KnowledgeList.tsx # 知识列表
│   │   │   ├── Editor.tsx        # Markdown 编辑器
│   │   │   ├── MetadataPanel.tsx # 元数据面板
│   │   │   ├── SearchOverlay.tsx # 搜索覆盖层
│   │   │   ├── GitPanel.tsx      # Git 状态面板
│   │   │   └── Notifications.tsx # AI 活动通知
│   │   ├── services/
│   │   │   ├── api.ts            # 统一 API 接口
│   │   │   ├── api.tauri.ts      # Tauri IPC 实现
│   │   │   └── api.http.ts       # HTTP 实现（Web端）
│   │   ├── hooks/
│   │   └── stores/
│   ├── package.json
│   └── vite.config.ts
└── README.md
```

### 1.3 核心依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| `tauri` | v2.x | 桌面应用框架 |
| `serde` / `serde_yaml` / `serde_json` | latest | 序列化 |
| `pulldown-cmark` | latest | Markdown 解析 |
| `git2` | latest | Git 操作（libgit2 绑定） |
| `notify` | latest | 文件系统监听 |
| `clap` | latest | CLI 参数解析 |
| `tokio` | latest | 异步运行时 |
| `axum` | latest | HTTP Server（可选） |
| `fs2` | latest | 跨平台文件锁（flock） |
| `sha2` | latest | 内容 hash（summary_hash） |
| `regex` | latest | 全文搜索正则匹配 |
| `rand` | latest | Token 随机生成 |
| `hex` | latest | Token hex 编码 |
| `tower-http` | latest | HTTP 中间件（CORS 等） |

**前端依赖：**

| 依赖 | 用途 |
|------|------|
| `react` | UI 框架 |
| `@tauri-apps/api` | Tauri IPC（含 `isTauri()` 检测） |
| `@milkdown/core` + 插件 | 所见即所得 Markdown 编辑器 |
| `tailwindcss` | 样式 |
| `zustand` | 状态管理 |
| `react-arborist` 或自研 | 树形导航组件 |

---

## 2. 核心模块设计

### 2.1 memoforge-core：知识管理引擎

#### 2.1.0 .memoforge 目录结构与 Git 排除策略

`.memoforge/` 目录存放知识库配置和运行时状态。需严格区分**持久配置**（可 Git 追踪）和**运行时文件**（必须排除）。

**目录结构：**

```
.memoforge/
├── config.yaml        # [持久] 知识库配置（分类注册等）
├── .gitignore         # [持久] 排除运行时文件
├── serve.pid          # [运行时] MCP Server PID
├── http.token         # [运行时] HTTP Bearer Token
├── events.jsonl       # [运行时] 事件日志（审计+通知）
├── git.lock           # [运行时] Git 操作全局锁
└── write.lock         # [运行时] 全局写锁（move_knowledge 引用更新时使用）
```

**`.memoforge/.gitignore`（知识库初始化时自动创建）：**

```gitignore
# 运行时状态 — 不追踪
serve.pid
http.token
events.jsonl
git.lock
*.lock
```

**实现要点：**

```rust
impl KnowledgeStore {
    /// 初始化知识库时自动创建 .memoforge/.gitignore
    fn ensure_gitignore(root: &Path) -> Result<(), MemoError> {
        let gitignore_path = root.join(".memoforge/.gitignore");
        if !gitignore_path.exists() {
            std::fs::write(&gitignore_path, MEMOFORGE_GITIGNORE)?;
        }
        Ok(())
    }
}

const MEMOFORGE_GITIGNORE: &str = "\
# MemoForge runtime files — do not track
serve.pid
http.token
events.jsonl
git.lock
*.lock
";
```

**`git_commit` 暂存策略：** 需要正确处理新增、修改、删除、重命名四种情况，同时排除 `.memoforge/` 下的运行时文件。核心原则：**依赖 `.memoforge/.gitignore` 排除运行时文件，而非手动选择性 add。**

```rust
fn git_commit_impl(store: &KnowledgeStore, message: &str) -> Result<CommitResult, MemoError> {
    // 前提：.memoforge/.gitignore 已正确配置，排除 serve.pid / http.token / events.jsonl / *.lock
    //
    // 1. git add -u   （暂存已追踪文件的修改和删除，覆盖 delete_knowledge / move_knowledge 的旧路径）
    // 2. git add .     （暂存新文件，.gitignore 自动排除运行时文件）
    //    等价于 git add -A，但因为有 .gitignore 保护，运行时文件不会被暂存
    // 3. git status --porcelain 检查是否有待提交内容
    //    如无变更 → 返回 "nothing to commit"（非错误）
    // 4. git commit -m message
    //
    // 安全兜底：在 git add 后、commit 前，检查 staged files 中是否包含
    // .memoforge/ 下除 config.yaml 和 .gitignore 外的文件，如有则 unstage 并警告
}
```

> **为什么不用手动 `git add **/*.md`：** 手动选择性 add 无法处理删除（`git add` 不暂存已删除文件）和重命名（旧路径的 deletion 不会被暂存）。`git add -u` + `git add .` 配合 `.gitignore` 是 Git 标准的安全暂存方式。

#### 2.1.1 KnowledgeStore

知识库的核心入口，管理知识的生命周期。

```rust
pub struct KnowledgeStore {
    root: PathBuf,                    // 知识库根目录
    config: KnowledgeConfig,          // .memoforge/config.yaml
    index: Arc<RwLock<FrontmatterIndex>>, // 内存索引缓存
    lock_manager: LockManager,        // 文件锁管理
    event_logger: EventLogger,        // 事件日志
}

impl KnowledgeStore {
    pub fn open(root: PathBuf) -> Result<Self, MemoError>;

    // L0: 列表
    pub fn list(&self, path: Option<&str>, tags: Option<&[String]>,
                depth: Option<u32>, cursor: Option<&str>, limit: Option<usize>)
        -> Result<ListResult, MemoError>;
        // depth 默认 1, limit 默认 200

    // L1: 摘要
    pub fn get_summary(&self, path: &str) -> Result<KnowledgeSummary, MemoError>;
```

**`KnowledgeSummary` 返回结构：**

```rust
pub struct KnowledgeSummary {
    pub path: String,
    pub title: String,
    pub tags: Vec<String>,
    pub summary: Option<String>,
    pub summary_hash: Option<String>,
    /// summary 是否过期：content_hash(当前正文) != summary_hash
    pub summary_stale: bool,
    pub related: Vec<String>,
    pub created: NaiveDate,
    pub updated: NaiveDate,
}

impl KnowledgeStore {
    /// summary_stale 计算逻辑：
    /// 1. 读取文件，解析 frontmatter 和正文
    /// 2. 计算 content_hash(正文)
    /// 3. 比对 content_hash 与 frontmatter.summary_hash
    /// 4. 若不一致或 summary_hash 为空，则 summary_stale = true
    fn compute_summary_stale(fm: &Frontmatter, body: &str) -> bool {
        match &fm.summary_hash {
            Some(hash) => content_hash(body) != *hash,
            None => fm.summary.is_some(), // 有 summary 但无 hash，视为 stale
        }
    }
}
```

```rust
    // L2: 全文
    pub fn get_content(&self, path: &str, section: Option<usize>)
        -> Result<KnowledgeContent, MemoError>;

    // 写入
    pub fn create(&self, path: &str, content: &str,
                  metadata: Option<FrontmatterPatch>) -> Result<Knowledge, MemoError>;
    pub fn update(&self, path: &str, content: Option<&str>,
                  metadata: Option<FrontmatterPatch>) -> Result<Knowledge, MemoError>;
    pub fn update_metadata(&self, path: &str, metadata: FrontmatterPatch)
        -> Result<KnowledgeSummary, MemoError>;
    pub fn delete(&self, path: &str, dry_run: bool)
        -> Result<DeleteResult, MemoError>;
    pub fn move_knowledge(&self, from: &str, to: &str, dry_run: bool)
        -> Result<MoveResult, MemoError>;

    // 搜索
    pub fn grep(&self, pattern: &str, path: Option<&str>,
                options: GrepOptions) -> Result<Vec<GrepMatch>, MemoError>;

    // 关联
    pub fn get_related(&self, path: &str) -> Result<Vec<RelatedKnowledge>, MemoError>;
    pub fn get_backlinks(&self, path: &str) -> Result<Vec<Backlink>, MemoError>;
    pub fn get_tags(&self, prefix: Option<&str>) -> Result<Vec<TagInfo>, MemoError>;

    // 分类
    pub fn create_category(&self, path: &str, label: &str) -> Result<Category, MemoError>;
    pub fn get_config(&self) -> &KnowledgeConfig;
}
```

#### 2.1.2 Frontmatter 解析

```rust
#[derive(Serialize, Deserialize, Clone)]
pub struct Frontmatter {
    pub title: String,
    #[serde(default)]
    pub tags: Vec<String>,
    #[serde(default)]
    pub summary: Option<String>,
    #[serde(default)]
    pub summary_hash: Option<String>,
    #[serde(default)]
    pub related: Vec<String>,
    pub created: NaiveDate,
    pub updated: NaiveDate,
}

/// 三态 Patch 值：区分"不更新"、"设为新值"、"清空为 null"
/// JSON 映射：字段缺失 = Absent，字段有值 = Set(value)，字段为 null = Clear
///   { "summary": "new text" }  → summary = Set("new text")
///   { "summary": null }        → summary = Clear
///   { }                        → summary = Absent（不动）
#[derive(Clone, Default)]
pub enum PatchValue<T> {
    #[default]
    Absent,       // 字段未出现 → 保持原值
    Set(T),       // 字段有值 → 更新为此值
    Clear,        // 字段为 null → 清空（对 Option 字段设为 None，对 Vec 字段设为空）
}

/// Patch 类型：用于 update/update_metadata 的增量更新。
/// AI Agent 调用 update_metadata 时只需传想改的字段：
///   { "summary": "new summary" }       → 只更新 summary
///   { "summary": null }                → 清空 summary
///   { "tags": ["rust", "async"] }      → 替换 tags
///   { "tags": null }                   → 清空 tags
#[derive(Clone, Default)]
pub struct FrontmatterPatch {
    pub title: PatchValue<String>,
    pub tags: PatchValue<Vec<String>>,
    pub summary: PatchValue<String>,
    pub related: PatchValue<Vec<String>>,
    // created 不可修改
    // updated 由系统自动设置为当前日期
    // summary_hash 在 summary 变更时自动重算
}

impl FrontmatterPatch {
    /// 将 patch 合并到现有 frontmatter
    pub fn apply_to(&self, fm: &mut Frontmatter, body: &str) -> Result<(), MemoError> {
        match &self.title {
            PatchValue::Set(v) => fm.title = v.clone(),
            PatchValue::Clear => {
                // title 必填，不允许清空 → 返回错误
                return Err(MemoError {
                    code: ErrorCode::InvalidFrontmatter,
                    message: "title is required and cannot be cleared".into(),
                    retry_after_ms: None,
                    context: None,
                });
            }
            PatchValue::Absent => {}
        }
        match &self.tags {
            PatchValue::Set(v) => fm.tags = v.clone(),
            PatchValue::Clear => fm.tags = vec![],
            PatchValue::Absent => {}
        }
        match &self.summary {
            PatchValue::Set(v) => {
                fm.summary = Some(v.clone());
                fm.summary_hash = Some(content_hash(body));
            }
            PatchValue::Clear => {
                fm.summary = None;
                fm.summary_hash = None;
            }
            PatchValue::Absent => {}
        }
        match &self.related {
            PatchValue::Set(v) => fm.related = v.clone(),
            PatchValue::Clear => fm.related = vec![],
            PatchValue::Absent => {}
        }
        fm.updated = chrono::Local::now().date_naive();
        Ok(())
    }
}

// serde 实现：利用 #[serde(default)] + 自定义 Deserialize
// JSON null → Clear, 字段缺失 → Absent, 有值 → Set(value)

/// 从 Markdown 文件解析 frontmatter + 正文
pub fn parse_knowledge_file(content: &str) -> Result<(Frontmatter, String), MemoError> {
    // 分离 --- ... --- 包裹的 YAML 和正文
}

/// 将 frontmatter + 正文合并为完整 Markdown
pub fn render_knowledge_file(fm: &Frontmatter, body: &str) -> String {
    // 生成 --- YAML --- + body
}

/// 计算正文内容的 hash（用于 summary_stale 检测）
pub fn content_hash(body: &str) -> String {
    // SHA256 取前 8 位 hex
}
```

#### 2.1.3 章节切分

```rust
pub struct Section {
    pub index: usize,
    pub title: String,
    pub content: String,    // 含子标题的完整内容
    pub level: u8,          // heading level (2 for ##)
}

/// 按 ## (h2) 标题切分文档
pub fn split_sections(body: &str) -> Vec<Section> {
    // # (h1) 视为文档标题，不参与切分
    // ## 开始一个新 section
    // section 包含其下的所有 ###/#### 内容
    // 按文档出现顺序编号 (0-indexed)
}
```

#### 2.1.4 双向链接解析

```rust
/// 从正文中提取 [[path]] 格式的链接
pub fn extract_wikilinks(body: &str) -> Vec<String> {
    // regex: \[\[([^\]]+\.md)\]\]
}

/// 扫描全库构建反向链接索引
pub fn build_backlink_index(root: &Path) -> HashMap<String, Vec<String>> {
    // key: 被引用的知识路径
    // value: 引用者的知识路径列表
}

/// 更新全库中引用了旧路径的文件（move_knowledge 时调用）
pub fn update_references(root: &Path, old_path: &str, new_path: &str)
    -> Result<Vec<String>, MemoError> {
    // 扫描所有 .md 文件的 frontmatter.related 和正文 [[old_path]]
    // 替换为 new_path
    // 返回受影响的文件列表
}
```

#### 2.1.5 并发控制

```rust
pub struct LockManager {
    root: PathBuf,
}

impl LockManager {
    /// 对知识文件加排他锁（写操作前调用）
    pub fn lock_file(&self, path: &str) -> Result<FileLock, MemoError> {
        // 使用 fs2::FileExt::lock_exclusive()
        // 非阻塞尝试，失败返回 CONFLICT_FILE_LOCKED
    }

    /// 对 Git 操作加全局锁
    pub fn lock_git(&self) -> Result<FileLock, MemoError> {
        // 锁 .memoforge/git.lock
        // 阻塞等待，超时 30s 后返回错误
    }

    /// 批量加锁：对多个文件同时加排他锁（move_knowledge 使用）
    /// 按路径字典序排列后逐个加锁，避免死锁
    pub fn lock_files(&self, paths: &[&str]) -> Result<BatchLock, MemoError> {
        let mut sorted = paths.to_vec();
        sorted.sort(); // 固定加锁顺序，防止死锁
        let mut locks = Vec::new();
        for path in &sorted {
            match self.lock_file(path) {
                Ok(lock) => locks.push(lock),
                Err(e) => {
                    // 加锁失败时释放已获取的所有锁（locks drop）
                    return Err(e);
                }
            }
        }
        Ok(BatchLock { locks })
    }

    /// 全局写锁：阻止所有文件写入（move_knowledge 引用更新阶段使用）
    /// 通过锁 .memoforge/write.lock 实现
    pub fn lock_global_write(&self) -> Result<FileLock, MemoError> {
        // 锁 .memoforge/write.lock
        // 短暂持有：仅在 update_references 期间
        // 其他写操作在获取 file lock 前先尝试获取 global_write 的共享锁
        // move_knowledge 获取排他锁，阻塞所有并发写入
    }
}

pub struct FileLock {
    file: std::fs::File,
    // Drop 时自动释放锁
}

pub struct BatchLock {
    locks: Vec<FileLock>,
    // Drop 时按反序释放所有锁
}
```

**move_knowledge 并发安全流程：**

```
move_knowledge(from, to, dry_run=false)
  │
  ├── 1. dry_run 阶段：扫描引用，返回影响列表（不加锁）
  │
  └── 2. 执行阶段：
      ├── a. 获取全局写锁 (lock_global_write)
      │      → 阻塞其他进程的所有写操作
      ├── b. 获取 from + to 的文件锁
      ├── c. 执行文件移动 (rename)
      ├── d. update_references: 逐个修改引用文件
      │      → 此时其他写入被全局写锁阻塞，不会出现覆盖冲突
      ├── e. 更新内存索引
      └── f. 释放全局写锁 + 文件锁
           → 其他写操作恢复
```

> **全局写锁的影响范围：** 仅在 move_knowledge 的引用更新阶段（步骤 d）持有，通常耗时 < 100ms（扫描所有 .md 文件的 frontmatter.related + 正文 wikilink）。普通的 create/update/delete 操作在获取单文件锁前，先尝试获取 `write.lock` 的共享锁（shared lock），共享锁之间不互斥，因此不影响正常并发写入。只有 move_knowledge 的排他锁会短暂阻塞。

#### 2.1.6 事件日志

```rust
pub struct EventLogger {
    path: PathBuf,  // .memoforge/events.jsonl
}

#[derive(Serialize)]
pub struct Event {
    pub time: DateTime<Utc>,
    pub source: String,      // "mcp:claude-code", "gui", "cli"
    pub action: String,      // "create", "update", "delete", "move", "git_commit" 等
    pub path: Option<String>,
    pub detail: String,
}

impl EventLogger {
    /// 追加一条事件（append 模式，行级原子性）
    pub fn log(&self, event: Event) -> Result<(), MemoError>;
    /// 读取最近 N 条事件
    pub fn recent(&self, limit: usize) -> Result<Vec<Event>, MemoError>;
}
```

#### 2.1.7 统一错误类型

```rust
#[derive(Debug, Serialize)]
pub struct MemoError {
    pub code: ErrorCode,
    pub message: String,
    pub retry_after_ms: Option<u64>,
    pub context: Option<serde_json::Value>,
}

#[derive(Debug, Serialize)]
pub enum ErrorCode {
    // 状态
    NotInitialized,      // 没有活跃的知识库（冷启动未完成或切换失败后）
    // 资源不存在
    NotFoundKnowledge,
    NotFoundCategory,
    // 参数错误
    InvalidPath,
    InvalidFrontmatter,
    // 并发冲突
    ConflictFileLocked,
    ConflictGitMerge,
    // Git
    GitAuthFailed,
    GitPushRejected,
    GitRemoteUnreachable,
    GitDirtyState,
    // 权限
    PermissionReadonly,
    // 限制
    LimitResultTooLarge,
}
```

### 2.2 memoforge-mcp：MCP Server

#### 2.2.1 CLI 入口

```rust
#[derive(Parser)]
#[command(name = "memoforge")]
enum Cli {
    /// 启动 MCP Server（AI Agent 接入）
    Serve {
        #[arg(long)]
        knowledge_path: PathBuf,
        /// MCP 接口的访问模式
        #[arg(long, default_value = "readwrite")]
        mode: AccessMode,
        /// 启动 HTTP Server
        #[arg(long)]
        http: bool,
        /// HTTP 接口的访问模式（独立于 MCP mode）
        #[arg(long, default_value = "readonly")]
        http_mode: AccessMode,
        #[arg(long, default_value = "8080")]
        port: u16,
        #[arg(long, default_value = "127.0.0.1")]
        bind: String,
        /// 指定 Bearer Token（省略则自动生成）
        #[arg(long)]
        token: Option<String>,
    },
}

/// MCP 和 HTTP 使用独立的 AccessMode
/// - MCP 默认 readwrite（AI Agent 需要读写能力）
/// - HTTP 默认 readonly（PRD §5.1.5: Web 默认只读）
/// - 如需 HTTP 可写：memoforge serve --http --http-mode readwrite
#[derive(Clone, ValueEnum)]
pub enum AccessMode {
    Readwrite,
    Readonly,
}
```

**HTTP 读写控制中间件：**

```rust
/// HTTP 路由层：根据 http_mode 决定是否注册写入路由
fn build_http_routes(store: Arc<KnowledgeStore>, http_mode: AccessMode) -> Router {
    let read_routes = Router::new()
        .route("/api/knowledge", get(list_knowledge))
        .route("/api/knowledge/:path/summary", get(get_summary))
        .route("/api/knowledge/:path", get(get_content))
        .route("/api/grep", get(grep))
        .route("/api/tags", get(get_tags))
        .route("/api/status", get(get_status))
        .route("/api/git/status", get(git_status))
        .route("/api/git/log", get(git_log));

    match http_mode {
        AccessMode::Readonly => read_routes,
        AccessMode::Readwrite => read_routes
            .route("/api/knowledge", post(create_knowledge))
            .route("/api/knowledge/:path", put(update_knowledge))
            // ... 其他写入路由
    }
}
```

> **设计原则：** readonly 模式不是在中间件里拦截 POST 返回 403，而是直接不注册写入路由（返回 404）。这样前端也不会误显示编辑按钮。前端通过 `/api/status` 返回的 `http_mode` 字段判断当前是否只读，隐藏编辑功能。

#### 2.2.2 HTTP Server 认证机制

> 对应 PRD §5.1.5 Web/HTTP 安全模型

**Token 生成与存储：**

```rust
use rand::Rng;

/// 生成随机 Bearer Token（prefix: mf_，32 字节 hex）
pub fn generate_token() -> String {
    let bytes: [u8; 32] = rand::thread_rng().gen();
    format!("mf_{}", hex::encode(bytes))
}

/// HTTP 启动时的 Token 逻辑
fn resolve_token(cli_token: Option<String>) -> String {
    match cli_token {
        Some(t) => t,  // 用户通过 --token 指定
        None => {
            let token = generate_token();
            // 写入 .memoforge/http.token 供前端读取
            // 打印到终端供用户复制
            eprintln!("Web access token: {} (pass via Authorization: Bearer header)", token);
            token
        }
    }
}
```

**认证中间件（axum）：**

```rust
use axum::{extract::Request, middleware::Next, response::Response, http::StatusCode};

pub async fn auth_middleware(
    req: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    let expected_token = req.extensions().get::<String>()
        .expect("token must be set");

    // 仅从 Authorization header 读取，不接受 URL query 参数
    let auth_header = req.headers()
        .get("authorization")
        .and_then(|v| v.to_str().ok());

    match auth_header {
        Some(header) if header.starts_with("Bearer ") => {
            let token = &header[7..];
            if token == expected_token.as_str() {
                Ok(next.run(req).await)
            } else {
                Err(StatusCode::UNAUTHORIZED)
            }
        }
        _ => Err(StatusCode::UNAUTHORIZED),
    }
}
```

**CORS 配置：**

```rust
use tower_http::cors::{CorsLayer, AllowOrigin};

/// CORS 策略：
/// - 同源请求不带 Origin header，浏览器不触发 CORS 检查，天然放行
/// - 跨域请求（不同 origin）默认被拒绝
/// - 仅在用户通过配置文件显式添加 allowed_origins 时才启用 CORS 层
fn build_cors(allowed_origins: &[String]) -> Option<CorsLayer> {
    if allowed_origins.is_empty() {
        // 默认：不添加 CORS 层 → 任何跨域请求都会被浏览器拦截
        // 同源请求不受影响（浏览器不发 preflight）
        None
    } else {
        // 用户配置了允许的 origins
        let origins: Vec<_> = allowed_origins.iter()
            .filter_map(|o| o.parse().ok())
            .collect();
        Some(CorsLayer::new()
            .allow_origin(AllowOrigin::list(origins))
            .allow_methods([Method::GET, Method::POST, Method::PUT, Method::DELETE])
            .allow_headers([AUTHORIZATION, CONTENT_TYPE]))
    }
}
```

> **为什么不用 `AllowOrigin::exact("null")`：** `Origin: null` 仅出现在 `file://` 协议或 sandboxed iframe 中，不是"同源"的含义。同源请求的浏览器行为是根本不带 Origin header、不触发 CORS preflight，因此不需要服务端做任何 CORS 配置。

**安全规则总结：**

| 规则 | 实现 |
|------|------|
| Token 来源 | 仅 `Authorization: Bearer <token>` header |
| URL query 传 token | 明确禁止，中间件不读取 query 参数 |
| 默认监听地址 | `127.0.0.1`（仅本机） |
| 跨域 | 默认禁止，需显式配置 |
| Token 持久化 | 写入 `.memoforge/http.token`，仅文件所有者可读（chmod 600） |

#### 2.2.3 MCP Tool 定义

每个 tool 的 description 需要精心设计，因为这是 AI Agent 理解接口的唯一途径。

```rust
/// Tool 注册示例
fn register_tools(server: &mut McpServer, store: Arc<KnowledgeStore>) {
    server.add_tool(Tool {
        name: "list_knowledge",
        description: r#"
List knowledge entries in the knowledge base (L0 level - titles and tags only).
Returns a directory/knowledge tree with pagination support.

Use this as the first step to explore the knowledge base structure.
For summaries, use get_summary. For full content, use get_content.

Parameters:
- path: Directory path relative to root (optional, default: root)
- tags: Filter by tags, comma-separated (optional)
- depth: Directory depth, 1=direct children only (optional, default: 1)
- cursor: Pagination cursor from previous response (optional)
- limit: Max entries per page (optional, default: 200)

Returns: { path, type, children: [{path, type, title, tags, updated}], total, next_cursor }

Error codes:
- NOT_FOUND_CATEGORY: path does not exist
- LIMIT_RESULT_TOO_LARGE: too many results, use depth/tags to filter
"#,
        handler: /* ... */,
    });

    server.add_tool(Tool {
        name: "create_knowledge",
        description: r#"
Create a new knowledge entry in the knowledge base.
The path MUST start with a registered category (use get_config to see available categories).
Frontmatter fields (title, created, updated) are auto-generated if not provided.

⚠️ This operation modifies the knowledge base.

Parameters:
- path: File path under a registered category (required, e.g. "programming/rust/new-topic.md")
- content: Markdown body content (required)
- metadata: { title?, tags?, summary?, related? } (optional)

Returns: { success, path, title, created }

Error codes:
- INVALID_PATH: path does not start with a registered category. Response includes available categories.
- CONFLICT_FILE_LOCKED: file is being edited by another process. Retry after retry_after_ms.
"#,
        handler: /* ... */,
    });

    server.add_tool(Tool {
        name: "delete_knowledge",
        description: r#"
Delete a knowledge entry. Default is dry-run mode (preview only).

⚠️ DESTRUCTIVE: This permanently removes the file. Use git to recover if needed.

Parameters:
- path: Knowledge file path (required)
- dry_run: If true (default), only preview impact without deleting

Returns (dry_run=true): { dry_run: true, impact, affected_files }
Returns (dry_run=false): { success: true, deleted }

Error codes:
- NOT_FOUND_KNOWLEDGE: file does not exist
- CONFLICT_FILE_LOCKED: file is locked by another process
"#,
        handler: /* ... */,
    });

    server.add_tool(Tool {
        name: "git_pull",
        description: r#"
Pull remote changes and merge into local. Run this before git_push.

If conflicts occur, returns conflict details. Resolve conflicts by:
1. Call get_content on conflicted files to read conflict markers
2. Call update_knowledge with resolved content
3. Call git_commit to complete the merge

Returns (success): { success: true, updated_files, new_commits }
Returns (conflict): { success: false, error_code: "CONFLICT_GIT_MERGE", conflicts: [{path, ours, theirs}] }

Error codes:
- GIT_AUTH_FAILED: check Git credentials
- GIT_REMOTE_UNREACHABLE: check network or remote URL
- GIT_DIRTY_STATE: repository in abnormal state (interrupted rebase, etc.)
"#,
        handler: /* ... */,
    });

    // ... 其他 tool 同理
}
```

#### 2.2.4 服务状态文件

```rust
/// 启动时写入 PID 文件
fn write_pid_file(root: &Path) -> Result<(), MemoError> {
    let pid_path = root.join(".memoforge/serve.pid");
    let info = json!({
        "pid": std::process::id(),
        "started_at": Utc::now().to_rfc3339(),
        "knowledge_path": root.display().to_string(),
    });
    std::fs::write(&pid_path, info.to_string())?;
    Ok(())
}

/// 退出时清理 PID 文件（通过 Drop 或 signal handler）
fn cleanup_pid_file(root: &Path) {
    let _ = std::fs::remove_file(root.join(".memoforge/serve.pid"));
}
```

### 2.3 memoforge-tauri：桌面应用

#### 2.3.1 Tauri Commands

Tauri Commands 封装 `memoforge-core`，暴露给 React 前端。所有 command 必须先检查是否有活跃知识库。

```rust
/// Guard wrapper：持有 RwLockReadGuard，只暴露 &KnowledgeStore
pub struct StoreGuard<'a> {
    _guard: tokio::sync::RwLockReadGuard<'a, Option<KnowledgeStore>>,
}

impl<'a> std::ops::Deref for StoreGuard<'a> {
    type Target = KnowledgeStore;
    fn deref(&self) -> &KnowledgeStore {
        // safe: active_store 已验证 is_some()
        self._guard.as_ref().unwrap()
    }
}

/// 获取当前活跃的 KnowledgeStore，无则返回 NOT_INITIALIZED
async fn active_store(state: &AppState) -> Result<StoreGuard<'_>, MemoError> {
    let guard = state.store.read().await;
    if guard.is_none() {
        return Err(MemoError {
            code: ErrorCode::NotInitialized,
            message: "没有活跃的知识库，请先创建或打开一个知识库".into(),
            retry_after_ms: None,
            context: None,
        });
    }
    Ok(StoreGuard { _guard: guard })
}

#[tauri::command]
async fn list_knowledge(
    state: tauri::State<'_, AppState>,
    path: Option<String>,
    tags: Option<Vec<String>>,
    depth: Option<u32>,
) -> Result<ListResult, MemoError> {
    let store = active_store(&state).await?;
    store.list(path.as_deref(), tags.as_deref(), depth, None, None)
}

#[tauri::command]
async fn get_content(
    state: tauri::State<'_, AppState>,
    path: String,
    section: Option<usize>,
) -> Result<KnowledgeContent, MemoError> {
    let store = active_store(&state).await?;
    store.get_content(&path, section)
}

// ... 其他 commands 同理，均通过 active_store() 获取
```

#### 2.3.2 文件系统监听

```rust
/// 文件监听器持有结构，必须挂载到 AppState 保证生命周期
pub struct KnowledgeWatcher {
    _watcher: notify::RecommendedWatcher,  // 持有 watcher 防止 drop
    _thread: std::thread::JoinHandle<()>,   // 持有后台线程
}

pub fn start_watcher(
    root: PathBuf,
    app_handle: tauri::AppHandle,
) -> Result<KnowledgeWatcher, MemoError> {
    let (tx, rx) = std::sync::mpsc::channel();

    let mut watcher = notify::recommended_watcher(tx)?;
    watcher.watch(&root, notify::RecursiveMode::Recursive)?;

    // 后台线程处理事件
    let thread = std::thread::spawn(move || {
        for event in rx {
            match event {
                Ok(event) => {
                    // 过滤 .git/ 目录的变更（忽略）
                    // 对 .md 文件变更：通知前端刷新
                    app_handle.emit("knowledge-changed", &event.paths).ok();

                    // 对 events.jsonl 变更：读取新事件，发送通知
                    if event.paths.iter().any(|p| p.ends_with("events.jsonl")) {
                        // 读取最新事件，发送 toast 通知
                        app_handle.emit("new-event", &latest_events).ok();
                    }
                }
                Err(e) => eprintln!("Watch error: {}", e),
            }
        }
    });

    // 返回 watcher 和线程句柄，调用方必须持有它们
    Ok(KnowledgeWatcher {
        _watcher: watcher,
        _thread: thread,
    })
}
```

**挂载到 AppState：**

```rust
pub struct AppState {
    pub store: RwLock<Option<KnowledgeStore>>,
    pub watcher: RwLock<Option<KnowledgeWatcher>>,
    pub global_config: RwLock<GlobalConfig>,  // 多知识库切换时需要修改
}

// Tauri 启动时
fn main() {
    tauri::Builder::default()
        .setup(|app| {
            let root = /* 从配置获取知识库路径 */;
            let store = KnowledgeStore::open(root.clone())?;
            let watcher = start_watcher(root, app.handle().clone())?;

            app.manage(AppState {
                store: RwLock::new(Some(store)),
                watcher: RwLock::new(Some(watcher)),
                global_config: RwLock::new(load_global_config()?),
            });
            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

#### 2.3.3 MCP 服务状态检测

```rust
pub fn check_mcp_status(root: &Path) -> McpStatus {
    let pid_path = root.join(".memoforge/serve.pid");
    match std::fs::read_to_string(&pid_path) {
        Ok(content) => {
            let info: serde_json::Value = serde_json::from_str(&content).unwrap_or_default();
            let pid = info["pid"].as_u64().unwrap_or(0) as u32;
            // 检查进程是否存活
            if process_alive(pid) {
                McpStatus::Running { pid, started_at: info["started_at"].as_str().unwrap_or("").into() }
            } else {
                // PID 文件残留，清理
                let _ = std::fs::remove_file(&pid_path);
                McpStatus::NotRunning
            }
        }
        Err(_) => McpStatus::NotRunning,
    }
}
```

---

## 3. 前端架构

### 3.1 API 分层（桌面/Web 共享）

```typescript
// services/api.ts — 统一接口
import { isTauri } from '@tauri-apps/api/core';
import * as tauriApi from './api.tauri';
import * as httpApi from './api.http';

const api = isTauri() ? tauriApi : httpApi;

// 只读接口 — 桌面/Web 共享
export const listKnowledge = api.listKnowledge;
export const getSummary = api.getSummary;
export const getContent = api.getContent;

// 写入接口 — Web 端根据 http_mode 决定是否可用
// 前端通过 /api/status 返回的 http_mode 字段判断
export const createKnowledge = api.createKnowledge;  // Web readonly 时返回 404
// ... 其他接口
```

```typescript
// services/api.http.ts — HTTP 实现（Web 端）
// 写入操作在 readonly 模式下，服务端不注册路由（返回 404）
// 前端通过 getServerStatus() 获取 http_mode，在 UI 层隐藏编辑功能

export async function getServerStatus(): Promise<StatusResult> {
    const res = await fetchWithAuth(`${BASE_URL}/api/status`);
    return res.json();
    // 返回 { ..., http_mode: "readonly" | "readwrite" }
    // 前端据此决定是否显示编辑按钮、新建入口等
}
```

```typescript
// services/api.tauri.ts — Tauri IPC 实现
import { invoke } from '@tauri-apps/api/core';

export async function listKnowledge(params: ListParams): Promise<ListResult> {
    return invoke('list_knowledge', params);
}

export async function getContent(path: string, section?: number): Promise<ContentResult> {
    return invoke('get_content', { path, section });
}
```

```typescript
// services/api.http.ts — HTTP 实现（Web 端）
const BASE_URL = window.location.origin;

/**
 * Token 获取策略：
 * 1. 优先从 URL hash fragment 读取（首次访问时由用户粘贴到地址栏）
 * 2. 存入 sessionStorage（同一标签页会话期间复用）
 * 3. 不存入 localStorage，关闭标签页后需重新输入
 */
function getToken(): string {
    // 优先从 sessionStorage 获取
    const cached = sessionStorage.getItem('memoforge_token');
    if (cached) return cached;

    // 从 URL hash 解析: http://localhost:8080/#token=mf_xxx
    const hash = window.location.hash;
    const match = hash.match(/token=([^&]+)/);
    if (match) {
        const token = match[1];
        sessionStorage.setItem('memoforge_token', token);
        // 清除 URL 中的 token，避免浏览器历史泄漏
        window.location.hash = '';
        return token;
    }

    // 弹出输入框要求用户粘贴 token
    const input = prompt('请输入 MemoForge 访问令牌 (在终端启动日志中查看)');
    if (input) {
        sessionStorage.setItem('memoforge_token', input);
        return input;
    }

    throw new Error('未提供访问令牌');
}

export async function listKnowledge(params: ListParams): Promise<ListResult> {
    const query = new URLSearchParams(params as any).toString();
    const res = await fetch(`${BASE_URL}/api/knowledge?${query}`, {
        headers: { 'Authorization': `Bearer ${getToken()}` }
    });
    if (res.status === 401) {
        sessionStorage.removeItem('memoforge_token');
        throw new Error('令牌无效或已过期，请刷新页面重新输入');
    }
    return res.json();
}
```

### 3.2 核心组件

#### CategoryTree — 分类树形导航

```typescript
interface TreeNode {
    path: string;
    label: string;
    icon?: string;
    type: 'category' | 'subcategory' | 'knowledge';
    children?: TreeNode[];
    count?: number;
    isExpanded?: boolean;
}
```

- 使用 `react-arborist` 或自研树组件
- 顶层分类从 `get_config()` 获取
- 展开时调用 `list_knowledge(path, depth=1)` 延迟加载子节点
- 当前选中项蓝紫色高亮
- 知识条目（叶子节点）无展开箭头

#### Editor — Markdown 编辑器

```typescript
// 集成 Milkdown 所见即所得编辑器
interface EditorProps {
    content: string;
    onChange: (content: string) => void;
    mode: 'wysiwyg' | 'source';  // 所见即所得 / 源码模式
}
```

- 默认所见即所得模式（Milkdown）
- 工具栏：加粗、斜体、标题、列表、代码块、链接、图片、表格
- 右上角切换到源码模式（纯文本编辑器，等宽字体）
- `[[` 输入触发自动补全（搜索知识库完整相对路径）

#### MetadataPanel — 元数据面板

- 编辑器顶部表单：标题输入框、标签 chips 输入、关联知识下拉选择
- 用户不接触 YAML 语法，表单数据自动映射到 frontmatter

---

## 4. MCP Tool Description 设计规范

MCP tool 的 description 是 AI Agent 理解接口能力的**唯一信息来源**，必须精确、完整、自解释。

### 4.1 Description 编写规范

每个 tool description 必须包含：

1. **一句话功能描述** — 做什么
2. **使用场景说明** — 什么时候用，与其他 tool 的关系
3. **参数说明** — 每个参数的含义、类型、是否必填、默认值
4. **返回值说明** — 成功和失败的返回结构
5. **错误码** — 可能出现的 error_code 及含义
6. **安全标注** — 写入/破坏性操作需要 ⚠️ 标注

### 4.2 完整 Tool Description 列表

#### get_status

```
Get knowledge base status and environment info.
Returns: initialization state, total knowledge count, Git status
(is_git_repo, branch, has_uncommitted_changes, remote_url),
MCP service info (pid, uptime), access mode, and HTTP mode.

Call this first to understand the knowledge base state before other operations.

Parameters: none

Returns: {
  initialized, knowledge_count,
  git: {is_repo, branch, uncommitted, remote_url},
  service: {pid, started_at},
  mode,
  http_mode  // "readonly" | "readwrite" | null (no HTTP server)
             // Web frontend uses this to show/hide editing UI
}
```

#### get_config

```
Get knowledge base configuration, including registered categories.
Use this to understand the knowledge base structure and find valid
category paths for create_knowledge.

Parameters: none

Returns: { name, categories: [{path, label, icon}] }
```

#### create_category

```
Register a new top-level category in the knowledge base.
Creates the directory and updates .memoforge/config.yaml.

⚠️ This operation modifies the knowledge base configuration.

Parameters:
- path: Category directory name (required, e.g. "devops")
- label: Display name (required, e.g. "运维部署")

Returns: { success, category: {path, label} }

Error codes:
- INVALID_PATH: category name contains invalid characters
- CONFLICT_FILE_LOCKED: config file is locked
```

#### list_knowledge

```
List knowledge entries in the knowledge base (L0 level - titles and tags only).
Returns a directory/knowledge tree with pagination support.

Use this as the FIRST STEP to explore the knowledge base structure.
For detailed summaries, use get_summary. For full content, use get_content.

Parameters:
- path: Directory path relative to root (optional, default: root)
- tags: Filter by tags, comma-separated (optional)
- depth: Directory depth, 1=direct children only (optional, default: 1)
- cursor: Pagination cursor from previous response (optional)
- limit: Max entries per page (optional, default: 200)

Returns: { path, type, children: [{path, type, title, tags, updated}], total, next_cursor }

Error codes:
- NOT_FOUND_CATEGORY: specified path does not exist
- LIMIT_RESULT_TOO_LARGE: too many results without filtering, add depth or tags parameter
```

#### get_summary

```
Get knowledge entry metadata and summary (L1 level).
Returns frontmatter including title, tags, summary, related links.
The summary_stale field indicates if the summary is outdated relative to content.

Use this to decide if you need the full content (get_content) or if the summary is sufficient.

Parameters:
- path: Knowledge file path (required, e.g. "programming/rust/async-patterns.md")

Returns: { path, title, tags, summary, summary_hash, summary_stale, related, created, updated }

Error codes:
- NOT_FOUND_KNOWLEDGE: file does not exist at specified path
```

#### get_content

```
Get full knowledge content (L2 level). Optionally retrieve a specific section.
Sections are split by ## (h2) headings, indexed from 0.

Parameters:
- path: Knowledge file path (required)
- section: Section index (optional, 0-based, corresponds to sections array)

Returns: { path, metadata: {...}, content, sections: ["section title 1", "section title 2", ...] }

Error codes:
- NOT_FOUND_KNOWLEDGE: file does not exist
```

#### grep

```
Full-text search across knowledge base using regex patterns.
Supports optional tag filtering for combined search (tag + keyword).
Returns matching lines with context preview.

Parameters:
- pattern: Regex search pattern (required if tags is omitted)
- tags: Filter by tags, comma-separated (optional, AND logic — all tags must match)
- path: Limit search to this directory (optional, default: entire knowledge base)
- options: { case_insensitive?: bool, context_lines?: number (default: 2), max_results?: number (default: 50) }

If both pattern and tags are provided, results are files matching ALL specified tags
AND containing text matching the pattern. If only tags is provided, returns matching
knowledge entries without line-level matches.

Returns: { matches: [{path, title, line_number, line_content, context_before, context_after}], knowledge_list: [{path, title, tags}], total_matches }
```

#### create_knowledge

```
Create a new knowledge entry in the knowledge base.
The path MUST start with a registered category (use get_config to see available categories).
Frontmatter fields (title, created, updated) are auto-generated if not provided.
File name should use kebab-case (e.g. "async-patterns.md").

⚠️ This operation creates a new file in the knowledge base.

Parameters:
- path: File path under a registered category (required, e.g. "programming/rust/new-topic.md")
- content: Markdown body content (required)
- metadata: { title?, tags?, summary?, related? } (optional)

Returns: { success, path, title, created }

Error codes:
- INVALID_PATH: path does not start with a registered category. Response includes available categories list.
- CONFLICT_FILE_LOCKED: another process is writing to this location. Retry after retry_after_ms.
```

#### update_knowledge

```
Update an existing knowledge entry's content and/or metadata.
Automatically updates the 'updated' date field.

⚠️ This operation modifies an existing file.

Parameters:
- path: Knowledge file path (required)
- content: New markdown body (optional, omit to keep current content)
- metadata: Frontmatter fields to update (optional, merged with existing)

Returns: { success, path, updated }

Error codes:
- NOT_FOUND_KNOWLEDGE: file does not exist
- INVALID_FRONTMATTER: metadata contains invalid values (e.g. title: null)
- CONFLICT_FILE_LOCKED: file is locked by another process. retry_after_ms indicates wait time.
```

#### update_metadata

```
Update only the frontmatter metadata of a knowledge entry.
Use this to write AI-generated summary, update tags, or set related links.
When updating summary, summary_hash is automatically recalculated.
Only include the fields you want to change — omitted fields are left unchanged.
Set a field to null to clear it (e.g. { "summary": null } removes the summary).

⚠️ This operation modifies an existing file.

Parameters:
- path: Knowledge file path (required)
- metadata: Frontmatter fields to update (required)
  - Omit a field → keep current value
  - Set a field → update to new value (e.g. { "summary": "new text" })
  - Set to null → clear the field (e.g. { "summary": null })
  - Note: title is required and cannot be set to null (returns INVALID_FRONTMATTER)

Returns: { success, path, updated, summary_stale }

Error codes:
- NOT_FOUND_KNOWLEDGE: file does not exist
- INVALID_FRONTMATTER: attempted to clear a required field (e.g. title: null)
- CONFLICT_FILE_LOCKED: file is locked
```

#### delete_knowledge

```
Delete a knowledge entry. Defaults to dry-run mode (preview only, no actual deletion).
Set dry_run=false to actually delete.

⚠️ DESTRUCTIVE: Permanently removes the file. Recoverable via Git if the knowledge base is a Git repo.

Parameters:
- path: Knowledge file path (required)
- dry_run: Preview mode (optional, **DEFAULT: true** — must explicitly set false to delete)

Returns (dry_run=true): { dry_run: true, impact: "...", affected_files: [{path, reference_type}] }
Returns (dry_run=false): { success: true, deleted: "path" }

Error codes:
- NOT_FOUND_KNOWLEDGE: file does not exist
- CONFLICT_FILE_LOCKED: file is locked
```

#### move_knowledge

```
Move or rename a knowledge entry. Automatically updates all references
(related fields and [[wikilinks]]) across the entire knowledge base.
Defaults to dry-run mode.

⚠️ This modifies multiple files across the knowledge base.
During execution, a brief global write lock is held to ensure reference
updates are atomic. Other write operations may be briefly delayed.

Parameters:
- from: Current file path (required)
- to: New file path (required, must be under a registered category)
- dry_run: Preview mode (optional, **DEFAULT: true** — must explicitly set false to move)

Returns (dry_run=true): { dry_run: true, affected_files: [{path, old_ref, new_ref}] }
Returns (dry_run=false): { success: true, moved: {from, to}, updated_references: count }

Error codes:
- NOT_FOUND_KNOWLEDGE: source file does not exist
- INVALID_PATH: target path not under registered category
- CONFLICT_FILE_LOCKED: source, target, or a referenced file is locked by another process
```

#### git_status

```
Get Git repository status. Returns branch, uncommitted changes, remote info.
Use this to understand the current Git state before commit/pull/push.

Parameters: none

Returns: { is_repo, branch, uncommitted_files: [{path, status}], remote_url, ahead, behind }
Returns (not a git repo): { is_repo: false }
```

#### git_commit

```
Commit all current changes to the local Git repository.

Parameters:
- message: Commit message (optional, auto-generated if omitted based on changed files)

Returns: { success, commit_hash, files_committed, message }

Error codes:
- GIT_DIRTY_STATE: repository in abnormal state
- CONFLICT_FILE_LOCKED: git.lock is held by another process
```

#### git_pull

```
Pull remote changes and merge into local. Run this BEFORE git_push.

If merge conflicts occur, returns conflict details with instructions:
1. Call get_content on conflicted files to read conflict markers
2. Call update_knowledge with resolved content
3. Call git_commit to complete the merge

Parameters: none

Returns (success): { success: true, updated_files: [...], new_commits: count }
Returns (conflict): { success: false, error_code: "CONFLICT_GIT_MERGE", conflicts: [{path, conflict_content}] }

Error codes:
- GIT_AUTH_FAILED: authentication failed, user should check credentials
- GIT_REMOTE_UNREACHABLE: cannot reach remote, check network
- GIT_DIRTY_STATE: repository in abnormal state
- CONFLICT_FILE_LOCKED: git.lock is held
```

#### git_push

```
Push local commits to remote repository. Defaults to dry-run mode.
Always call git_pull first to ensure local is up to date.

⚠️ This affects the remote repository and is visible to collaborators.

Parameters:
- dry_run: Preview mode (optional, **DEFAULT: true** — must explicitly set false to push)

Returns (dry_run=true): { dry_run: true, commits_to_push: count, remote_url }
Returns (dry_run=false): { success: true, pushed_commits: count }

Error codes:
- GIT_PUSH_REJECTED: remote has new commits, call git_pull first
- GIT_AUTH_FAILED: authentication failed
- GIT_REMOTE_UNREACHABLE: cannot reach remote
- CONFLICT_FILE_LOCKED: git.lock is held
```

#### git_log

```
View commit history for a file or the entire knowledge base.

Parameters:
- path: File path for file-specific history (optional, omit for global history)
- limit: Number of commits to return (optional, default: 10)

Returns: { commits: [{hash, message, author, date, files_changed}] }
```

#### get_related

```
Get knowledge entries related to the specified entry.
Combines explicit related field links and [[wikilinks]] found in content.

Parameters:
- path: Knowledge file path (required)

Returns: { related: [{path, title, relation_type}] }
  relation_type: "explicit" (from related field) | "wikilink" (from [[]] in content) | "mutual" (both directions)
```

#### get_tags

```
List all tags used in the knowledge base with usage counts.

Parameters:
- prefix: Filter tags starting with this prefix (optional)

Returns: { tags: [{name, count}] }
```

#### get_backlinks

```
Find all knowledge entries that reference the specified entry.
Searches both related fields and [[wikilinks]] across the entire knowledge base.

Parameters:
- path: Knowledge file path (required)

Returns: { backlinks: [{path, title, reference_type}] }
  reference_type: "related" (in related field) | "wikilink" (in [[]] content)
```

---

## 5. 数据流

### 5.1 AI Agent 典型工作流

```
Agent                          MemoForge MCP
  │                                │
  ├─── get_status() ──────────────►│  了解知识库状态
  │◄── {initialized, count, git} ──│
  │                                │
  ├─── list_knowledge(depth=1) ───►│  L0: 浏览分类
  │◄── {categories + titles} ──────│
  │                                │
  ├─── get_summary(path) ─────────►│  L1: 查看摘要
  │◄── {summary, tags, stale} ─────│
  │                                │
  ├─── get_content(path) ─────────►│  L2: 获取全文（仅在需要时）
  │◄── {content, sections} ────────│
  │                                │
  ├─── create_knowledge(...) ─────►│  创建新知识
  │◄── {success, path} ────────────│
  │                                │
  ├─── git_commit("add: ...") ────►│  提交
  │◄── {commit_hash} ─────────────│
  │                                │
  ├─── git_push(dry_run=false) ───►│  推送
  │◄── {success} ──────────────────│
```

### 5.2 文件写入流程

```
调用方 (MCP/GUI)
  │
  ├── 1. 验证路径（分类是否已注册）
  │
  ├── 2. 获取文件锁 (flock)
  │   ├── 成功 → 继续
  │   └── 失败 → 返回 CONFLICT_FILE_LOCKED
  │
  ├── 3. 读取现有文件（如更新操作）
  │
  ├── 4. 修改内容/frontmatter
  │   ├── 自动更新 updated 日期
  │   ├── 如更新 summary → 重算 summary_hash
  │   └── 如更新 content → summary_hash 不变（summary_stale=true）
  │
  ├── 5. 写入文件
  │
  ├── 6. 释放文件锁
  │
  ├── 7. 写入 events.jsonl
  │
  └── 8. 更新内存索引缓存
```

---

## 6. 快速跟进 / Should Have 阶段功能设计

> 以下功能不在 Day 1 发布范围，但 PRD 明确要求，提前完成技术设计以减少后续返工。

### 6.1 导入已有 Markdown 文件夹

> 对应 PRD §5.1.6 / §9.2

```rust
pub struct ImportOptions {
    /// 来源目录（被扫描的目录）
    pub source: PathBuf,
    /// 目标分类路径（必须是已注册分类）
    pub target_category: String,
    /// 是否递归子目录
    pub recursive: bool,
    /// 是否覆盖已存在的同名文件
    pub overwrite: bool,
}

pub struct ImportResult {
    pub imported: Vec<String>,   // 成功导入的文件路径
    pub skipped: Vec<String>,    // 已存在而跳过的
    pub errors: Vec<(String, String)>, // (文件名, 错误原因)
}

impl KnowledgeStore {
    /// 导入已有 Markdown 文件夹
    ///
    /// 处理逻辑：
    /// 1. 扫描 source 目录中所有 .md 文件
    /// 2. 对每个文件：
    ///    a. 尝试解析 frontmatter
    ///    b. 若无 frontmatter，自动生成：
    ///       - title: 从文件名推断（kebab-case → 空格分隔，首字母大写）
    ///       - created/updated: 从文件时间戳获取
    ///       - tags: 空
    ///       - summary: None
    ///    c. 将文件复制到 target_category 对应目录下
    /// 3. 更新内存索引缓存
    pub fn import_directory(&self, options: ImportOptions)
        -> Result<ImportResult, MemoError>;
}
```

**自动 frontmatter 生成示例：**

源文件 `my-notes/rust_tips.md`（无 frontmatter）导入到分类 `programming` 后：

```yaml
---
title: "Rust Tips"
tags: []
created: 2025-11-15    # 从文件创建时间获取
updated: 2026-01-20    # 从文件修改时间获取
---
```

### 6.2 多知识库管理

> 对应 PRD §5.1.6 / §9.2

**全局配置文件**（`~/.memoforge/global.yaml`）：

```yaml
# 全局配置：管理多个知识库
knowledge_bases:
  - path: /Users/chenpu/knowledge/personal
    label: "个人知识库"
    default: true
  - path: /Users/chenpu/knowledge/team
    label: "团队知识库"
  - path: /Users/chenpu/projects/memo-rust
    label: "Rust 学习笔记"

last_active: /Users/chenpu/knowledge/personal
```

**实现要点：**

```rust
pub struct GlobalConfig {
    pub knowledge_bases: Vec<KnowledgeBaseEntry>,
    pub last_active: Option<PathBuf>,
}

pub struct KnowledgeBaseEntry {
    pub path: PathBuf,
    pub label: String,
    pub default: bool,
}
```

> AppState 的权威定义见 §2.3.2。多知识库切换复用同一 AppState 结构。

```rust
// AppState 定义见 §2.3.2，统一结构：
// pub struct AppState {
//     pub store: RwLock<Option<KnowledgeStore>>,
//     pub watcher: RwLock<Option<KnowledgeWatcher>>,
//     pub global_config: RwLock<GlobalConfig>,
// }

impl AppState {
    /// 切换知识库：先构造新 store + watcher，成功后再原子替换旧状态
    pub async fn switch_knowledge_base(&self, path: &Path, app_handle: &tauri::AppHandle)
        -> Result<(), MemoError> {
        // 1. 先构造新 store 和 watcher（不修改任何现有状态）
        //    如果这一步失败，旧知识库仍可正常使用
        let new_store = KnowledgeStore::open(path.to_path_buf())?;
        let new_watcher = start_watcher(path.to_path_buf(), app_handle.clone())?;

        // 2. 成功后，原子替换旧状态（drop 旧 watcher → drop 旧 store）
        *self.watcher.write().await = Some(new_watcher);
        *self.store.write().await = Some(new_store);

        // 3. 更新 global_config.last_active
        self.global_config.write().await.last_active = Some(path.to_path_buf());
        save_global_config(&*self.global_config.read().await)?;

        // 4. 通知前端刷新全部数据
        app_handle.emit("knowledge-base-switched", path.display().to_string()).ok();
        Ok(())
    }

    /// 添加知识库并切换到它
    pub async fn add_and_switch(&self, path: String, store: KnowledgeStore,
                                 app_handle: &tauri::AppHandle) -> Result<(), MemoError> {
        // 1. 添加到 global_config.knowledge_bases
        // 2. 调用 switch_knowledge_base
    }
}
```

**Tauri Commands：**

```rust
#[tauri::command]
async fn list_knowledge_bases(state: tauri::State<'_, AppState>)
    -> Result<Vec<KnowledgeBaseEntry>, MemoError>;

#[tauri::command]
async fn switch_knowledge_base(state: tauri::State<'_, AppState>, path: String)
    -> Result<(), MemoError>;

#[tauri::command]
async fn add_knowledge_base(state: tauri::State<'_, AppState>,
    path: String, label: String) -> Result<(), MemoError>;

#[tauri::command]
async fn remove_knowledge_base(state: tauri::State<'_, AppState>,
    path: String) -> Result<(), MemoError>;
```

**MCP Server 多知识库**：MCP Server 通过 `--knowledge-path` 参数指定单个知识库，不支持运行时切换。用户如需 AI Agent 访问多个知识库，需启动多个 `memoforge serve` 进程，各自注册为不同的 MCP Server。

### 6.3 标签组合搜索

> 对应 PRD §9.2

在现有 `grep` 基础上扩展，支持标签过滤和组合查询：

```rust
pub struct SearchQuery {
    /// 全文搜索模式（正则）
    pub pattern: Option<String>,
    /// 标签过滤（AND 语义：所有指定标签都必须匹配）
    pub tags: Option<Vec<String>>,
    /// 搜索范围（目录路径）
    pub path: Option<String>,
    /// 搜索选项
    pub options: GrepOptions,
}

pub struct GrepOptions {
    pub case_insensitive: bool,
    pub context_lines: usize,   // 默认 2
    pub max_results: usize,     // 默认 50
}

impl KnowledgeStore {
    /// 组合搜索：支持 pattern + tags 联合过滤
    ///
    /// 执行逻辑：
    /// 1. 若指定 tags → 先从内存索引筛选出包含这些标签的文件路径集合
    /// 2. 若指定 pattern → 在（步骤1的结果集 或 全库）中执行正则搜索
    /// 3. 若同时指定 → 先 tag 过滤缩小范围，再 regex 搜索（性能优化）
    /// 4. 若仅指定 tags → 返回匹配的知识列表（不含行级匹配）
    pub fn search(&self, query: SearchQuery) -> Result<SearchResult, MemoError>;
}

pub struct SearchResult {
    /// 全文匹配结果（有 pattern 时）
    pub matches: Vec<GrepMatch>,
    /// 标签匹配的知识列表（仅 tags 时，或作为 matches 的补充上下文）
    pub knowledge_list: Vec<KnowledgeSummary>,
    pub total: usize,
}
```

**MCP 接口扩展**：在现有 `grep` tool 中新增 `tags` 参数，而非创建新 tool：

```
grep — Full-text search across knowledge base using regex patterns.
       Supports optional tag filtering for combined search.

Parameters:
- pattern: Regex search pattern (required if tags is omitted)
- tags: Filter by tags, comma-separated (optional, AND logic — all tags must match)
- path: Limit search to this directory (optional)
- options: { case_insensitive?, context_lines?, max_results? }

Note: If both pattern and tags are provided, results are files matching ALL specified tags
AND containing text matching the pattern.
```

### 6.4 冷启动路径技术设计

> 对应 PRD §3 场景6 / §5.1.6 / §9.1（Day 1 Must Have）

冷启动有三条路径，共用同一个 `KnowledgeInit` 模块：

```rust
pub enum InitMode {
    /// 从零创建新知识库（可选模板）
    New {
        path: PathBuf,
        template: Option<String>,  // "developer", "tech-notes", None(空白)
        git_remote: Option<String>, // 可选关联远程仓库
    },
    /// Clone 已有 Git 仓库
    Clone {
        remote_url: String,
        local_path: PathBuf,
    },
    /// 打开已存在的本地目录（可能是手工创建的 Markdown 文件夹）
    Open {
        path: PathBuf,
    },
}

pub struct KnowledgeInit;

impl KnowledgeInit {
    /// 统一入口：根据 InitMode 执行不同的初始化流程
    pub fn init(mode: InitMode) -> Result<KnowledgeStore, MemoError> {
        match mode {
            InitMode::New { path, template, git_remote } => {
                Self::init_new(&path, template.as_deref(), git_remote.as_deref())
            }
            InitMode::Clone { remote_url, local_path } => {
                Self::init_clone(&remote_url, &local_path)
            }
            InitMode::Open { path } => {
                Self::init_open(&path)
            }
        }
    }

    /// 路径 A：从零创建
    fn init_new(path: &Path, template: Option<&str>, git_remote: Option<&str>)
        -> Result<KnowledgeStore, MemoError>
    {
        // 1. 创建目录
        // 2. 创建 .memoforge/config.yaml（空或从模板）
        // 3. 创建 .memoforge/.gitignore（排除运行时文件）
        // 4. 如有模板 → init_from_template()
        // 5. git init
        // 6. 如有 git_remote → git remote add origin <url>
        // 7. 首次 commit: "Initial knowledge base"
        // 8. KnowledgeStore::open(path)
    }

    /// 路径 B：Clone Git 仓库
    fn init_clone(remote_url: &str, local_path: &Path)
        -> Result<KnowledgeStore, MemoError>
    {
        // 1. git2::Repository::clone(remote_url, local_path)
        // 2. 校验 .memoforge/config.yaml 是否存在
        //    - 存在 → 合法的 MemoForge 知识库
        //    - 不存在 → 创建默认 config.yaml + .gitignore（视为导入场景）
        // 3. KnowledgeStore::open(local_path)
        //
        // 错误处理：
        // - GIT_AUTH_FAILED: 认证失败（提示配置 SSH key 或 token）
        // - GIT_REMOTE_UNREACHABLE: 网络不可达
        // - INVALID_PATH: local_path 已存在且非空
    }

    /// 路径 C：打开已有目录
    fn init_open(path: &Path)
        -> Result<KnowledgeStore, MemoError>
    {
        // 1. 校验目录是否存在
        // 2. 如有 .memoforge/config.yaml → 直接 KnowledgeStore::open()
        // 3. 如无 → 创建 .memoforge/ 目录 + 默认 config.yaml + .gitignore
        //    提示用户可通过 GUI 注册分类
        // 4. 如非 Git 仓库 → 提示（不强制 git init）
        // 5. KnowledgeStore::open(path)
    }
}
```

**Tauri Commands（冷启动向导）：**

```rust
#[tauri::command]
async fn create_knowledge_base(
    state: tauri::State<'_, AppState>,
    path: String,
    template: Option<String>,
    git_remote: Option<String>,
) -> Result<(), MemoError> {
    let store = KnowledgeInit::init(InitMode::New {
        path: path.clone().into(),
        template,
        git_remote,
    })?;
    // 更新 global config + 切换到新知识库
    state.add_and_switch(path, store).await
}

#[tauri::command]
async fn clone_knowledge_base(
    state: tauri::State<'_, AppState>,
    remote_url: String,
    local_path: String,
) -> Result<(), MemoError> {
    let store = KnowledgeInit::init(InitMode::Clone {
        remote_url,
        local_path: local_path.clone().into(),
    })?;
    state.add_and_switch(local_path, store).await
}

#[tauri::command]
async fn open_knowledge_base(
    state: tauri::State<'_, AppState>,
    path: String,
) -> Result<(), MemoError> {
    let store = KnowledgeInit::init(InitMode::Open {
        path: path.clone().into(),
    })?;
    state.add_and_switch(path, store).await
}
```

### 6.5 模板知识库

> 对应 PRD §5.1.6 / §9.1（Day 1 Must Have，基础模板随冷启动流程一起交付）

```rust
pub struct Template {
    pub name: String,          // "developer", "tech-notes", "empty"
    pub label: String,         // "开发者知识库", "技术笔记", "空白知识库"
    pub description: String,
}

/// 内置模板列表
pub fn builtin_templates() -> Vec<Template>;

/// 从模板初始化知识库
pub fn init_from_template(root: &Path, template_name: &str)
    -> Result<(), MemoError>;
```

**模板内容以 Rust `include_str!` 嵌入二进制**，不依赖外部文件：

```rust
// 编译期嵌入模板内容
const DEVELOPER_TEMPLATE_CONFIG: &str = include_str!("templates/developer/config.yaml");
const DEVELOPER_TEMPLATE_WELCOME: &str = include_str!("templates/developer/welcome.md");
const DEVELOPER_TEMPLATE_EXAMPLE: &str = include_str!("templates/developer/example-knowledge.md");
```

**「开发者知识库」模板结构：**

```
.memoforge/
  config.yaml          # 预注册分类: programming, architecture, devops, tools
programming/
  welcome.md           # 欢迎知识，介绍如何使用 MemoForge
architecture/
devops/
tools/
  example-knowledge.md # 示例知识条目（展示 frontmatter 格式）
```

---

## 7. 构建与部署

### 7.1 开发环境

```bash
# 安装依赖
rustup install stable
cargo install tauri-cli
npm install       # 前端依赖

# 开发模式
cargo tauri dev   # 启动桌面应用（热重载）

# MCP Server 开发
cargo run -p memoforge-mcp -- serve --knowledge-path ./test-kb
```

### 7.2 构建产物

```bash
# 构建桌面应用
cargo tauri build
# 产物: target/release/bundle/
#   ├── macos/MemoForge.app
#   ├── dmg/MemoForge.dmg
#   ├── linux/memoforge.AppImage
#   └── windows/MemoForge.msi

# 构建 MCP Server（独立二进制）
cargo build -p memoforge-mcp --release
# 产物: target/release/memoforge
```

### 7.3 安装后的文件布局

```
/usr/local/bin/memoforge         # MCP Server CLI（也可用 cargo install）
/Applications/MemoForge.app/     # macOS 桌面应用
```

用户在 Claude Code 中配置 MCP 时指向 `memoforge` 二进制路径即可。

---

## 8. 关联文档

- [产品需求文档 (PRD)](./PRD.md)
- [讨论记录](./讨论.md)
- UI 设计：Pencil 项目文件
- [开发计划文档](./开发计划文档.md)
